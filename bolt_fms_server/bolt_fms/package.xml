<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>bolt_fms</name>
  <version>0.0.1</version>
  <description>FMS system with multiple ROS 2 nodes</description>
  <maintainer email="kdjin11@naver.com">gyucheol</maintainer>
  <license>Apache-2.0</license>

  <!-- 🔧 빌드 도구 -->
  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- 🔧 C++ 의존성 -->
  <depend>rclcpp</depend>
  <depend>std_msgs</depend>
  <depend>geometry_msgs</depend>

  <!-- 🐍 Python 노드를 위한 의존성 -->
  <depend>rclpy</depend>
  <depend>PySide6</depend>

  <!-- ✅ 메시지 인터페이스가 제거된 상태라면 아래 2개는 주석 또는 제거 -->
  <build_depend>rosidl_default_generators</build_depend>
  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>
  
  <!-- package.xml -->
  <build_depend>nlohmann_json</build_depend>
  <exec_depend>nlohmann_json</exec_depend>

  <!-- 🧪 테스트 의존성 -->
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <!-- 🔧 Export -->
  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
